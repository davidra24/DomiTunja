<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="OrangeRatingBar" parent="@android:style/Widget.Holo.RatingBar.Small">
        <item name="android:minWidth">10dip</item>
        <item name="android:maxWidth">30dip</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">30dip</item>
        <item name="android:progressDrawable">@drawable/ratingbar_star_orange</item>
    </style>
</resources>
